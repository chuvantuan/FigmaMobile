{
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --host 192.168.171.97 --port 3000"
  }
}